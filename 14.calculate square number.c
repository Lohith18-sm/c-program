#include <stdio.h>

int main() {
    int num, square;
    printf("Enter a number:\n");
    scanf("%d", &num);
    square = num * num;
    printf("The square number is: %d\n", num, square);

    return 0;
}
